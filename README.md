# Cricket-League-Score-Prediction
A machine learning project that predicts final cricket innings scores using IPL ball-by-ball data (2008–2017). Implements data preprocessing, feature engineering, and a deep neural network achieving ~14 MAE, with an interactive real-time prediction widget.
